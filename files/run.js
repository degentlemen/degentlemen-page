;(function($) {
	$(function() {
/*        $('#access').scrollNav({
            hooks: {
                onChange: function($link) {
                    _gaq.push([ '_trackPageview', '/' + $link.attr('href').substr(1) ]);
                }
            }
        }); */
/*
		$('input[type="address"]').each(function() {
			var $map = $( $(this).attr('usemap') );
			var ap = $(this).addresspicker({elements: {map: $map.selector}});
			var gmark = ap.addresspicker('marker');
			if (gmark && gmark.setVisible) gmark.setVisible(true);
			ap.addresspicker('updatePosition');
		});
*/
	});
})(jQuery);